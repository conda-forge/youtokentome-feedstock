{% set name = "youtokentome" %}
{% set version = "1.0.4" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/youtokentome-{{ version }}.tar.gz
  sha256: c5692c1f2949f8dced4718949f846233cbe1b515cb5a5ab9c191481d0ed40df0

build:
  number: 0
  entry_points:
    - yttm = youtokentome.yttm_cli:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('cxx') }}
  host:
    - cython
    - pip
    - python
  run:
    - click >=7.0
    - python

test:
  imports:
    - youtokentome
  commands:
    - pip check
    - yttm --help
  requires:
    - pip

about:
  home: https://github.com/vkcom/youtokentome
  summary: Unsupervised text tokenizer focused on computational efficiency
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - tyrius02
